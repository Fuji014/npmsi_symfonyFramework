{{ include('includes/AdminParts/header.html.twig') }}
<div class="main-content-inner">
<!-- data table start -->
                    <div class="col-12 mt-5">
                        <div class="card">
                            <div class="card-body">

                                <h4 class="header-title pull-left">Table Shiptype</h4>
                                <a href="{{ path('shiptype_create') }}" class="btn btn-rounded btn-warning mb-3 pull-right"><span class="fa fa-plus-circle"></span> Add New Ship</a>
                                <div class="data-tables">
                                    <table id="dataTable" class="text-center">
                                        <thead class="bg-light text-capitalize">
                                            <tr>
                                                <th>ShipNumber</th>
                                                <th>ShipName</th>
                                                <th>ShipContent</th>
                                                <th>ShipImage</th>
                                                <th>Action</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                        {% for shiptype in shiptype %}
                                            <tr>
                                                <td><p title="{{shiptype.Id}}" id="Dont">{{shiptype.Id}}</p></td>
                                                <td><p title="{{shiptype.Shipname}}" id="Dont">{{shiptype.Shipname}}</p></td>
                                                <td><p title="{{shiptype.Shipcontent}}" id="Dont">{{shiptype.Shipcontent}}</p></td>
                                                <td><p title="{{shiptype.Shipcontent}}" id="Dont">{{shiptype.Shipimage}}</p></td>
                                                <td>
                                                <a href='{{ path('shiptype_edit',{ id:shiptype.Id }) }}' title='View Record'><span class='fa fa-eye'></span></a>
                                                <a href='{{ path('shiptype_edit',{id:shiptype.Id}) }}' title='Update Record'><span class='fa fa-pencil-square-o'></span></a>
                                                <a href='#' title='Delete Record' class="delete-shiptype" data-id="{{ shiptype.Id }}"><span class='fa fa-trash'></span></a>
                                                </td>
                                            </tr>
                                        {% endfor %}
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>


                    

                    <!-- data table end -->
                    <!-- Primary table start -->

</div>
{{ include('includes/AdminParts/footer.html.twig') }}
